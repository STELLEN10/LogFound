"use client";

import { Bot, Command, Compass, Github, Keyboard, Mic, PlayCircle, Search, Settings2, Sparkles, Users, X } from "lucide-react";
import type { Route } from "next";
import Link from "next/link";
import { useCallback, useEffect, useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { Onboarding } from "@/components/experience/onboarding";
import { useKeyboardShortcuts } from "@/hooks/use-keyboard-shortcuts";

const commands = [{ label: "Open dashboard", detail: "Your founder workspace", Icon: Compass, path: "/" }, { label: "Ask Nova", detail: "Open the strategy room", Icon: Sparkles, path: "/intelligence" }, { label: "Open AI workspace", detail: "Run the secured GPT-5.6 layer", Icon: Bot, path: "/ai" }, { label: "Open GitHub Intelligence", detail: "Understand repository movement", Icon: Github, path: "/github" }, { label: "Launch Voice Workspace", detail: "Think with the full context in mind", Icon: Mic, path: "/voice" }, { label: "Replay a decision", detail: "Trace the story behind the work", Icon: PlayCircle, path: "/replay" }, { label: "Open collaboration", detail: "Projects, people, and shared memory", Icon: Users, path: "/collaboration" }, { label: "Open settings", detail: "Theme, voice, privacy, and shortcuts", Icon: Settings2, path: "/settings" }, { label: "Start product tour", detail: "A guided Logfound introduction", Icon: Keyboard, path: "tour" }];

export function AppShell({ children }: Readonly<{ children: React.ReactNode }>) {
  const [commandOpen, setCommandOpen] = useState(false); const [query, setQuery] = useState(""); const [onboardingOpen, setOnboardingOpen] = useState(false);
  const openCommand = useCallback(() => { setQuery(""); setCommandOpen(true); }, []);
  const openTour = useCallback(() => setOnboardingOpen(true), []);
  const results = useMemo(() => { const term = query.toLowerCase(); return commands.filter((item) => `${item.label} ${item.detail}`.toLowerCase().includes(term)); }, [query]);
  useEffect(() => { if (!localStorage.getItem("logfound-onboarded")) setOnboardingOpen(true); window.addEventListener("logfound:command", openCommand); window.addEventListener("logfound:onboarding", openTour); return () => { window.removeEventListener("logfound:command", openCommand); window.removeEventListener("logfound:onboarding", openTour); }; }, [openCommand, openTour]);
  const go = (path: string) => { setCommandOpen(false); if (path === "tour") { openTour(); return; } window.location.assign(path); };
  useKeyboardShortcuts([{ key: "k", meta: true, handler: openCommand }, { key: "v", meta: true, shift: true, handler: () => window.location.assign("/voice") }, { key: "p", meta: true, shift: true, handler: () => window.location.assign("/replay") }, { key: "w", meta: true, shift: true, handler: () => window.location.assign("/replay#weekly-review") }, { key: "d", meta: true, shift: true, handler: () => window.location.assign("/replay#founder-dna") }, { key: "f", meta: true, shift: true, handler: () => window.location.assign("/collaboration") }, { key: "l", meta: true, shift: true, handler: () => window.location.assign("/collaboration") }, { key: "t", meta: true, shift: true, handler: () => window.location.assign("/collaboration") }, { key: "/", handler: openCommand }, { key: "Escape", handler: () => setCommandOpen(false) }]);
  return <div className="min-h-screen bg-background text-foreground"><a href="#main-content" className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-50 focus:rounded-md focus:bg-primary focus:px-4 focus:py-2 focus:text-primary-foreground">Skip to content</a><header className="sticky top-0 z-30 flex h-16 items-center justify-between border-b border-border/60 bg-background/75 px-5 backdrop-blur-xl sm:px-6"><div className="flex min-w-0 items-center gap-3"><div className="flex size-8 shrink-0 items-center justify-center rounded-lg bg-primary text-primary-foreground shadow-[0_0_22px_hsl(var(--primary)/0.25)]"><Sparkles className="size-4" aria-hidden="true" /></div><Link href="/" className="font-semibold tracking-tight">Logfound</Link><span className="hidden rounded-full border border-primary/20 bg-primary/[0.07] px-2 py-0.5 text-[10px] font-medium uppercase tracking-[0.14em] text-primary sm:block">Demo</span><nav className="hidden items-center gap-1 xl:flex"><Link href="/intelligence" className="rounded-md px-2 py-1 text-sm text-muted-foreground transition-colors hover:bg-accent hover:text-foreground">Intelligence</Link><Link href={"/github" as Route} className="rounded-md px-2 py-1 text-sm text-muted-foreground transition-colors hover:bg-accent hover:text-foreground">GitHub</Link><Link href={"/voice" as Route} className="rounded-md px-2 py-1 text-sm text-muted-foreground transition-colors hover:bg-accent hover:text-foreground">Voice</Link><Link href={"/replay" as Route} className="rounded-md px-2 py-1 text-sm text-muted-foreground transition-colors hover:bg-accent hover:text-foreground">Replay</Link><Link href={"/collaboration" as Route} className="rounded-md px-2 py-1 text-sm text-muted-foreground transition-colors hover:bg-accent hover:text-foreground">Collaborate</Link></nav></div><div className="flex items-center gap-1.5"><Button variant="ghost" size="sm" onClick={openCommand} aria-label="Open Command Center"><Command className="mr-2 size-4" />Command <kbd className="ml-2 rounded border border-border px-1.5 py-0.5 text-[10px] text-muted-foreground">Ctrl K</kbd></Button><Link href={"/settings" as Route} className="flex size-9 items-center justify-center rounded-md text-muted-foreground transition-colors hover:bg-accent hover:text-foreground" aria-label="Open settings"><Settings2 className="size-4" /></Link></div></header><main id="main-content" className="animate-rise">{children}</main><Onboarding open={onboardingOpen} close={() => setOnboardingOpen(false)} />{commandOpen && <div className="fixed inset-0 z-40 flex items-start justify-center bg-background/80 p-4 pt-[12vh] backdrop-blur-md" role="dialog" aria-modal="true" aria-label="Command Center" onClick={() => setCommandOpen(false)}><section className="w-full max-w-2xl overflow-hidden rounded-2xl border border-border bg-card shadow-2xl" onClick={(event) => event.stopPropagation()}><div className="flex items-center gap-3 border-b border-border px-5 py-4"><Search className="size-5 text-primary" /><input autoFocus value={query} onChange={(event) => setQuery(event.target.value)} className="w-full bg-transparent text-base outline-none placeholder:text-muted-foreground" placeholder="Search your workspace, navigate, or run a command…" /><button onClick={() => setCommandOpen(false)} aria-label="Close Command Center"><X className="size-4 text-muted-foreground" /></button></div><div className="p-3"><p className="px-2 pb-2 pt-1 text-[10px] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Command Center</p>{results.map((item) => <button key={item.label} onClick={() => go(item.path)} className="group flex w-full items-center gap-3 rounded-xl p-3 text-left transition-colors hover:bg-accent"><span className="flex size-9 items-center justify-center rounded-lg border border-border bg-background text-primary"><item.Icon className="size-4" /></span><span className="min-w-0"><span className="block text-sm font-medium">{item.label}</span><span className="mt-0.5 block text-xs text-muted-foreground">{item.detail}</span></span><span className="ml-auto text-xs text-muted-foreground opacity-0 transition-opacity group-hover:opacity-100">Open</span></button>)}{results.length === 0 && <p className="px-3 py-10 text-center text-sm text-muted-foreground">No matching command. Try “voice”, “replay”, “settings”, or “GitHub”.</p>}</div><div className="flex items-center justify-between border-t border-border px-5 py-3 text-xs text-muted-foreground"><span>Search · navigate · run actions</span><span>Esc to close</span></div></section></div>}</div>;
}
